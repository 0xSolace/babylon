echo "Running pre-push checks..."

# Run linting only - typecheck and build have known issues in vendor workspace
bun run lint
if [ $? -ne 0 ]; then
  echo "Linting failed. Fix issues before pushing."
  exit 1
fi

echo "All pre-push checks passed."

