#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

echo "Formatting staged files with Biome..."
bunx lint-staged
if [ $? -ne 0 ]; then
  echo "❌ Biome formatting failed. Fix issues before committing."
  exit 1
fi
