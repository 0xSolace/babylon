# Changelog

All notable changes to the Babylon project are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

---

## [Unreleased]

### Added

- **Agent skills generation and docs integration**
  - **Why**: We expose A2A and MCP; agents (Cursor, Claude Code, ClawHub, etc.) need a single, up-to-date reference. Hand-maintained docs drift from code; generating from source keeps skills and endpoints in sync.
  - **Script** `scripts/generate-skills-md.ts`: Reads `packages/a2a` (babylon-agent-card, executor operations) and `packages/mcp` (tool list); writes a full Agent Skills package to `skills/babylon/` (SKILL.md with frontmatter, claw.json, README).
  - **npm scripts** `skills:generate` (skills markdown), `skills:package` (full package).
  - **docs:generate** now runs the skills generator after vendor doc pulls.
- **Outbound RSS feeds**
  - **Why**: Let users and tools subscribe to Babylon content (hot posts, breaking news) in standard RSS readers without duplicating feed logic.
  - **GET /feed/rss**: RSS 2.0 feed of hot posts. Reuses `/api/feed/hot` internally so scoring, caching, and filtering stay in one place; this route only converts JSON → XML.
  - **GET /feed/breaking-news/rss**: RSS 2.0 feed of breaking news (world events, org updates, actor posts). Reuses `/api/feed/widgets/breaking-news` the same way.
  - Shared RSS builder in `apps/web/src/lib/rss.ts` (RSS 2.0 XML with escaping, RFC 1123 dates, 5‑min cache headers). **Why single helper**: Consistent escaping and cache semantics across endpoints.
  - Feed layout exposes both feeds via `<link rel="alternate" type="application/rss+xml" ...>` and Next.js `metadata.alternates` so readers and crawlers can discover them.
- **Inbound RSS config (default sources)**
  - **Why**: "Where do we put RSS feed URLs?" should have one answer; runtime enable/disable should stay in the DB so we can turn feeds off without a deploy.
  - Default list moved from `game-bootstrap-service.ts` to `packages/engine/src/config/rss-sources.ts` as `DEFAULT_RSS_SOURCES`. Bootstrap seeds `rssFeedSources` from it; engine continues to read only from DB. Add or edit default feed URLs in that config file.

- **Public API tiered rate limiting**
  - **Why**: Public GET endpoints (feeds, markets, profiles, etc.) were previously unrate-limited. That allowed unbounded anonymous traffic, increasing cost and abuse risk. We now apply tiered limits so anonymous callers are capped per IP while authenticated users and API keys get higher quotas.
  - New configs in `@babylon/api` rate limiting:
    - **Read endpoints**: 20 req/min per IP (unauthenticated), 60 req/min per user (authenticated or API key), 10 req/min shared when IP cannot be determined.
    - **Firehose (SSE)**: 5 connections/min per IP (unauthenticated), 20/min per user, 2/min shared when IP unknown.
  - New helper `publicRateLimit(request, kind?)` in `@babylon/api`: runs optional auth, then rate limits by `userId` (if authed) or by client IP (otherwise), or by shared anonymous bucket if IP is missing. Returns `{ error, user, rateLimitInfo }` so handlers can avoid double-auth and attach standard headers on success.
  - New helper `addPublicReadHeaders(response, rateLimitInfo)`: sets `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset` and `Cache-Control: public, s-maxage=5, stale-while-revalidate=10` on successful public read responses. **Why**: Clients can respect limits before hitting 429; CDNs can cache and reduce origin load.
  - All public read-only GET routes (posts, markets, trending, reputation, registry, NFT, NPC, stats, SSE stats, onboarding check-username, questions dynamics, etc.) now call `publicRateLimit()` at the top and attach rate limit + cache headers on success. Auth-only routes (e.g. user search) still use `authenticate()` but apply `publicRateLimit()` first so unauthenticated attempts are rate limited by IP before returning 401.
  - **Null-user safety**: Endpoints that allow unauthenticated access were audited so that `user` is never dereferenced without a null check and query parameters (e.g. `userId`, `following`) are not trusted for authorization—only the authenticated identity from the token/API key is used for user-scoped data.
- **Public firehose token**
  - **GET /api/realtime/public-token**: Issues a short-lived token scoped only to public SSE channels (`feed`, `markets`, `breaking-news`, `upcoming-events`). No authentication required. **Why**: Enables read-only clients (dashboards, embeds) to subscribe to the public firehose without logging in, while keeping DMs and notifications behind the authenticated token endpoint. Rate limited with the firehose tier (5/min per IP) to prevent abuse of token issuance.

### Changed

- **RLS helpers off the `@babylon/db` barrel**
  - **`asUser` / `asSystem` / `asPublic`** are imported from **`@babylon/db/engine-storage`** (or **`@babylon/db/runtime`** only under exempt prefixes such as `apps/cli/` / `scripts/`), not from **`@babylon/db`**. **Why:** Keeps “this runs under RLS / system / public” obvious at the import site; the barrel stays types + named queries + `withTransaction`.
- **Root `scripts/` maintenance CLIs** import **`db`**, table symbols, and RLS helpers from **`@babylon/db/engine-storage`** instead of **`@babylon/db/runtime`**. **Why:** Aligns with the same boundary as `packages/api` / `packages/agents`; **`@babylon/db/runtime`** remains the internal entry for `packages/db` and for codemods that target web imports.
- **Database package boundaries (`@babylon/db`, `drizzle-orm`, `@babylon/db/runtime`)**
  - **Why:** Re-exporting Drizzle helpers (`eq`, `and`, `sql`, …) from `@babylon/db` created a misleading “second API,” tied the whole monorepo to duplicate import paths, and made it harder to enforce “SQL lives in the DB package.” Import SQL builders from **`drizzle-orm`**; import **types, named query functions, and helpers** from **`@babylon/db`**; import **`db`, table symbols, and RLS helpers** from **`@babylon/db/engine-storage`** (or **`@babylon/db/runtime`** only where the DB-boundary script exempts the file path).
  - **Codemod:** `scripts/split-drizzle-imports-from-db-barrel.ts` rewrites imports using **brace-balanced** parsing (non-greedy regexes were unsafe when a `}` appeared inside nested objects above the import).
  - **Lint:** Removed the Biome rule that forbade `drizzle-orm` in `packages/a2a` *because* it depended on the old re-exports; the “no `@babylon/db/tables` in a2a” restriction remains.
  - **Docs:** `docs/database-layer.md` (rationale and patterns), `docs/roadmap-database-layer.md` (follow-up work). Root `README.md` links to the database-layer doc.
- **Daily topic & NPC group-chat onboarding queries** consolidated under `packages/db` (`daily-topic-queries.ts`, `npc-group-chat-onboarding-queries.ts`) with **`asSystem`** used inside the DB layer for RLS.
  - **Why:** Cron/engine paths have no logged-in user; they need a **system** Postgres principal. Keeping queries in `@babylon/db` centralizes `where` clauses and avoids scattering RLS-sensitive SQL across `packages/engine`.
- **`engine-read-queries.ts`**, **`perp-price-impact-queries.ts`**, **`portfolio-pnl-queries.ts`**: engine reads and P&L SQL moved from `packages/engine` (including removal of unused `packages/engine/src/db/queries.ts`).
  - **Why:** Same audit/reuse story; `PortfolioPnLSnapshot` is now canonical from `@babylon/db` (engine `client.ts` re-exports the type only).
- **`event-market-linker-queries.ts`**, **`npc-positions-context-queries.ts`**, **`portfolio-breakdown-queries.ts`**: world-event ↔ market reads, NPC position bundle load, and portfolio breakdown read model moved from `event-market-linker`, `npc-positions-context-service`, and `portfolio-breakdown` (valuation and impact math stay in engine).
- **`market-decision-engine-queries.ts`**, **`earned-points-queries.ts`**, **`event-market-pipeline-queries.ts`**, **`npc-memory-queries.ts`**, **`npc-running-bit-queries.ts`**: active-question / recent-post reads, earned-points sync and awards, org-state modifier + sentiment updates, NPC memory/relationship/activity optimistic transactions, and running-bit actor state CRUD moved from `MarketDecisionEngine`, `earned-points-service`, `event-market-pipeline`, `npc-memory-service`, and `npc-running-bit-service` (parsers, pricing formulas, and `TotalPointsService.markDirty` stay in engine).
- **`db-state-store-queries.ts`**, **`game-context-cache-queries.ts`**: tick `DbStateStore` question/market/post/world-event SQL and cron `GameContextCache` continuous-game / active-question / recent-event reads moved from `packages/engine` into `@babylon/db` (mapping and cache TTL logic stay in engine).
- **`total-points-queries.ts`**: `TotalPointsService` recompute (single `asSystem` + engine-supplied valuation callback), dirty flags, zero-total batch mark, snapshot pagination, dirty-user listing/clear, and raw-SQL bulk backfill moved into `@babylon/db` (perp validity, prediction pricing, and logging stay in engine).
- **`game-bootstrap-queries.ts`**: `GameBootstrapService` state id reads, actor/org seed + sync transactions, minimum-balance top-ups, pool creation, NPC user inserts, continuous game row, RSS seeding, perp snapshot seeding, and stats counts moved into `@babylon/db` (capital allocation, tier thresholds, `StaticDataRegistry`, and bootstrap logging stay in engine).
- **`game-service-queries.ts`**: recent posts / posts-by-actor, active questions, aggregated stats, all games list, continuous game row read, **`recordStockPriceUpdateAsSystem`**, **`listAllActorStatesAsSystem`**, **`fetchActorStateRowAsSystem`**, **`createPostWithAllFieldsAsSystem`**, and **`fetchStockPriceHistoryAsSystem`** (widget cache 24h change) moved from **`getDbInstance()`** in `game-service`, `price-update-service`, `initial-investment-service`, `post-generation-helpers`, `game-tick`, **`cached-database-service`**, and tests into `@babylon/db` with **`asSystem`**; `getCompanies()` uses **`listAllOrganizationStatesAsSystem`**. **`@babylon/agents`** perp/org slices use **`listOrganizationStatesByPriceDescForWorldContext`** / **`listAllOrganizationStatesAsSystem`** instead of **`getDbInstance()`**.
- **`group-chat-queries.ts`**: invite-context read, user group-chat list, agent-owner membership check, kick-evaluation load, member deactivate, sweep member ids, all group chat ids, and running-average quality update moved from `group-chat-service` into `@babylon/db` with **`asSystem`** (invite/kick probabilities, random rolls, and `recordNpcGroupChatInviteTransaction` orchestration stay in engine).
- **`following-mechanics-queries.ts`**: follow chance context, follow upsert + interaction flags, active-follow reads/updates, proactive-player/reaction/engagement batches, and unfollow sweep loads moved from `following-mechanics` into `@babylon/db` (probability math, shuffling, caps, and notifier wiring stay in engine).
- **`fee-queries.ts`**: referrer lookup (`asSystem` + transaction client), trading-fee insert, trader `totalFeesPaid` bump, referral credit + balance transaction, referral earnings aggregation, and platform fee stats moved from `fee-service` into `@babylon/db` (fee rates, `FeeType`, `withTransaction` orchestration, and logging stay in engine).
- **`relationship-evolution-queries.ts`**: bidirectional/ordered `ActorRelationship` reads, initial/evolved inserts and updates, top relationships for prompts, `NPCInteraction` insert and recent list moved from `RelationshipEvolutionEngine` into `@babylon/db` (LLM prompts, simulation guard, and `StaticDataRegistry` mapping stay in engine).
- **`earned-points-queries.ts`**: `awardEarnedPointsForPnLAsSystem` and `awardBonusPointsAsSystem` centralize the last `asSystem` wrappers for PnL/bonus awards; `earned-points-service` no longer imports `asSystem` from `@babylon/db/runtime` (transaction path unchanged; `TotalPointsService.markDirty` after bonus still runs in engine for the non-tx path).
- **`dm-queries.ts`**: DM pre-create / retry lookup, transactional chat + participants + `DMAcceptance` bundle, and system message insert moved from `dm-service` into `@babylon/db` (agent/owner guardrails and logging stay in engine; creation uses `withTransaction` from runtime).
- **`actor-social-actions-queries.ts`**: recent `UserInteraction` pairs (`selectDistinctOn`), group membership by owner, non-group chat participant listing, sample continuous group chat, and actor–user DM + first message transaction moved from `ActorSocialActions` into `@babylon/db` (probabilities, templates, and `GroupChatService.recordInvite` stay in engine).
- **`alpha-group-invite-queries.ts`**: decay read, membership/limit/cooldown/weekly/activity counts, decline update (via engine-supplied `nextEligible` callback), aggregate invite stats, and decline analytics moved from `alpha-group-invite-service` into `@babylon/db` (thresholds, rolls, and `TieredGroupService` stay in engine).
- **`market-metrics-queries.ts`**: prediction market slice (active markets, position counts, price history) and perp slice (`StockPrice` + optional `PerpMarketSnapshot` with missing-table handling) moved from `market-metrics-service` into `@babylon/db` (volatility, prompt text, and registry enrichment stay in engine).
- **`npc-engagement-stats-queries.ts`**: dashboard engagement totals and 24h counts (`fetchNpcEngagementStats` with **`asSystem`**) moved from `npc-social-engagement-service` into `@babylon/db`.
- **`npc-social-engagement-queries.ts`**: recent-post / quoted-author / reaction / share / comment thread reads, like + comment + `NPCInteraction` inserts, and atomic share + repost (`withTransaction`) for `processNPCSocialEngagements` moved from runtime **`db`** into `@babylon/db` with **`asSystem`** / **`withTransaction`**; **`fetchActorRelationshipBidirectional`** trace labels extended for NPC social (`npc-social-*`). LLM prompts and probability math stay in engine.
- **`tag-queries.ts`**: post tag upsert bundle, tag-by-post and posts-by-tag reads, statistics window slice (join `Post` for deleted filter), related-tag display names, current trending snapshot with relations, and batched `TrendingTag` insert (`insertTrendingTagRows` + **`withTransaction`** from runtime) moved from `tag-service` into `@babylon/db` with **`asSystem`** (LLM tag extraction, trending score math, and logging stay in engine).
- **`tiered-group-queries.ts`**: NPC tier group slice reads, parent-id backfill, bootstrap insert bundle (group + chat + owner member/participant), membership lookups, invite/promote/demotion **`withTransaction`** steps, promotable/demotion scan lists, and global analytics aggregation moved from `tiered-group-service` into `@babylon/db` with **`asSystem`** on standalone reads/writes (distributed locks, `StaticDataRegistry`, and tier rules stay in engine).
- **`wallet-queries.ts`**: balance stats / virtual-balance / full user reads, transaction history listing, in-transaction balance + `BalanceTransaction` writes, lifetime PnL update, and initial deposit bundle moved from `wallet-service` into `@babylon/db` (**`asSystem`** for standalone reads; `withTransaction` + tx helpers unchanged for debit/credit/PnL; `InsufficientFundsError`, cache invalidation, and `TotalPointsService.markDirty` stay in engine).
- **`reputation-calculation-queries.ts`**: `AgentPerformanceMetrics` CRUD-style reads/patches, user PnL/deposit slice for trading metrics, `Feedback` insert + fetch, and leaderboard agent + user profile batch load moved from `reputation-calculation-service` into `@babylon/db` with **`asSystem`** (composite scoring, auto-feedback text, and `recalculateReputation` math stay in engine; leaderboard no longer N+1s users).
- **`narrative-event-processor-queries.ts`**: arc state transitions (optimistic + simple), arc plan schedule read/update, question text/number reads, world event insert (standalone + in-tx), arc row loads, optimistic event-count bump in **`withTransaction`**, and arc create/id lookup moved from `narrative-event-processor` into `@babylon/db` (**`asSystem`** / **`Transaction`**; templates, structured events, and market/article follow-ups stay in engine).
- **`user-alpha-group-assignment-queries.ts`**: signup eligibility read, reuse of **`countUserActiveNpcGroupMemberships`**, followed-actor ids, existing NPC owner ids, Tier 3 group slices (with chat + counts), member lookup, reactivate + chat-participant upsert, capacity-guarded insert bundle, and Tier 3 capacity stats moved from `user-alpha-group-assignment-service` into `@babylon/db` (**`asSystem`** / **`withTransaction`**; sorting, `TieredGroupService.ensureAllTiersExist`, and static registry stay in engine).
- **`npc-actor-wallet-queries.ts`**: atomic `ActorState.tradingBalance` debit (with numeric guard), credit, and balance read helpers for `createNpcWalletAdapter` (`DrizzleClient | Transaction`; `WalletPort` implementation and logging stay in engine). **`npcActorStateAtomicDebitAsSystem`**, **`npcActorStateAtomicCreditAsSystem`**, and **`selectNpcActorTradingBalanceAsSystem`** run the same SQL under **`asSystem`** when no transaction client is passed (default NPC wallet path). `initial-investment-service` uses **`npcActorStateAtomicDebitAsSystem`** for the seed balance step.
- **`initial-investment-queries.ts`**: `Pool` existence check, `ON CONFLICT DO NOTHING` NPC pool insert, `PoolPosition` insert, and `NPCTrade` insert for seed investments moved from repository-style `db.pool` / `db.poolPosition` / `db.npcTrade` calls into `@babylon/db` with **`asSystem`** (LLM batching and **`npcActorStateAtomicDebit`** on runtime `db` remain in engine; org/actor list + single-org state use **`listAllOrganizationStatesAsSystem`** / **`listAllActorStatesAsSystem`** / **`fetchOrganizationStateRowAsSystem`**).
- **`npc-investment-queries.ts`**: `ActorState` balance reads, `PoolPosition` / `PerpPosition` slices, baseline batch reads (all actor balances, open prediction pool ids, open perp user/org pairs, org states by id, relationship slices), de-risk / drawdown / opportunistic pool position loads, and active `Pool` listing for `NPCInvestmentManager` moved from inline Drizzle on runtime `db` into `@babylon/db` with **`asSystem`** (metrics, risk, and **`TradeExecutionService`** stay in engine).
- **`question-manager-queries.ts`**: public `WorldEvent` slice by time window, active / resolved-question prompt slices, trending-tag join, latest `questionNumber`, and `Question` insert+return for `QuestionManager` (`generateQuestionsForContinuousGame`, `generateTimeframeQuestion`) moved from runtime `db` into `@babylon/db` with **`asSystem`** (LLM prompts, arc planner, **`PredictionMarketService`** stay in engine).
- **`npc-group-dynamics-queries.ts`**: group-chat listing, tier join slice, participant/message/pool-position reads, reply-guy follow/comment/like/share counts, `UserInteraction` + `ActorRelationship` slices for joins/leaves/invite modifier, invite candidate listing (`Share` distinct users + `User` filter), NPC kick context, group-stats counts, and **writes** (atomic join/leave/message/bootstrap/invite/kick per **`asSystem`** call) moved from `npc-group-dynamics-service` into `@babylon/db`; the service no longer imports runtime **`db`** or **`drizzle-orm`** for this flow (LLM prompts, rolls, and snowflake id generation stay in engine).
- **`post-generation-queries.ts`**: shared `Post` / public `WorldEvent` window load for `loadSharedPostContext`, open `PoolPosition` slice for NPC position prompts, NPC discourse post slice (by author ids), bidirectional pair `NPCInteraction` history, `Question` lookup by `questionNumber`, and reuse of **`createPostWithAllFieldsAsSystem`** / **`insertNpcEngagementNpcInteraction`** for reply/quote writes moved from `post-generation-helpers` into `@babylon/db` with **`asSystem`**; **`fetchActorRelationshipBidirectional`** trace label **`post-gen-discourse-relationship`** added. `post-generation-helpers` no longer imports runtime **`db`**.
- **`game-tick-queries.ts`**: continuous-game slice + last-tick touch, active questions/pools, bootstrap counts (trending/relationships, posts/tagged posts), sample tag + trending inserts, tick token stats insert, perp snapshot/org base/open-position reads for NPC trade repricing, validation slices, oracle commit/reveal question updates, markets widget upsert + pool/market slices, world-facts latest + marker insert, volatility snapshot batch updates, arc-state batch preload, payout resolve reads + on-chain market fields, moved from `game-tick.ts` into `@babylon/db` with **`asSystem`**. Prediction payout resolution uses **`withTransaction`** from **`@babylon/db`** in **`game-tick.ts`** for **`CorePredictionMarketService`**; position count + question/timeframed follow-up SQL runs via **`Transaction`** helpers (**`countPredictionPositionsForMarketInTx`**, **`markQuestionResolvedAfterPayoutInTx`**, **`markTimeframedMarketResolvedForQuestionInTx`**) in **`game-tick-queries.ts`**.
- **`trade-execution-queries.ts`**: `OrganizationState` price read, `PerpPosition` / `PoolPosition` loads, open-prediction back-compat upsert + `NPCTrade`, CAS and legacy prediction closes, legacy pool settle with `ActorState` credit, and analytics `NPCTrade` inserts moved from `trade-execution-service` into `@babylon/db` with **`asSystem`** / **`withTransaction`** (`PerpMarketService` / `PredictionMarketService`, rate limits, and broadcasts stay in engine). NPC actor lookup uses **`fetchActorStateRowAsSystem`**; prediction NPC **`WalletPort`** reuses **`createNpcWalletAdapter`** (no runtime **`db`** in **`trade-execution-service`**).
- **`world-facts-queries.ts`**: all `WorldFact` list/insert/update/delete/toggle SQL moved from `world-facts-service.ts` into `@babylon/db` with **`asSystem`**; engine keeps simulation mocks and key/label generation only.
- **`onchain-market-queries.ts`**, **`agent-trade-notification-queries.ts`**, **`organization-price-queries.ts`**: load/update for on-chain market linking, agent-owner lookup for trade DMs, and org price clamp + `OrganizationState` upsert moved out of `onchain-market-service`, `agent-trade-notification`, and `price-update-service`.
- **`article-persistence-queries.ts`**, **`game-onboarding-queries.ts`**: article `Post` insert / image URL update and all `GameOnboarding` SQL moved from `article-persistence` and `game-onboarding-service`. **`fetchOrganizationIdByTicker`** added to `organization-price-queries` for `player-influence-service`.
- **`reply-rate-limiter-queries.ts`**, **`oracle-commitment-queries.ts`**, **`message-quality-queries.ts`**: NPC reply rate / streak SQL, oracle commitment CRUD, and message-quality recent-text + score aggregates moved from `reply-rate-limiter`, `oracle-commitment-store`, and `message-quality-checker` (crypto and scoring stay in engine).
- **`rss-feed-queries.ts`**: RSS source listing, headline dedupe/insert, fetch touch, untransformed headlines (`NOT EXISTS` parody), and old-headline cleanup moved from `rss-feed-service`. **`posting-probability-service`** now uses existing **`getActorsByIds`** / **`getRecentWorldEvents`** from `engine-read-queries` instead of inline Drizzle.
- **`parody-headline-queries.ts`**, **`arc-context-queries.ts`**: parody insert/list/mark-used and arc+question read transaction moved from `parody-headline-generator` and `arc-context-service`. **`fetchOrganizationStatePriceSlicesByIds`** added to `organization-price-queries` for market cascade reads in `market-correlation-service`.
- **`trending-calculation-queries.ts`**, **`article-rate-limiter-queries.ts`**: latest trending run time, prompt-context rows, tag name batch lookup, and article window counts moved from `trending-calculation-service` and `article-rate-limiter`. **`event-reaction-service`** DB fallback now uses **`getRecentWorldEvents`** from `engine-read-queries`.
- **`world-context-queries.ts`**: unresolved prediction markets, active questions ordered by `createdAt`, NPC/agent recent trade slices (with `users` join), and **`listOrganizationStatesByPriceDescForWorldContext`** (replaces `getDbInstance().getOrganizationsByPrice()` in `generateCurrentMarkets`).
- **`signal-extraction-queries.ts`**: question/posts/authors load for `SignalExtractionService` moved into one `asSystem` helper. **`random-context-queries.ts`**: active markets, post time-window sample, and recent world events moved from `prompts/random-context.ts`. **`random-context.ts`** gainers/losers now use **`listAllOrganizationStatesAsSystem`** from **`organization-price-queries`** (no `getDbInstance()`).
- **`organization-price-queries.ts`**: **`listAllOrganizationStatesAsSystem`** — shared `asSystem` select of all `OrganizationState` rows for **`listAllOrganizationStatesForMarketContext`** (delegates to it), random-context gainers/losers, and related engine paths; **`fetchOrganizationStateRowAsSystem`** for single-row reads (e.g. initial investment). **`packages/engine` no longer imports `getDbInstance()`.**
- **`timeframe-arc-queries.ts`**: active `TimeframedMarket` pagination and arc/event counter updates moved from `TimeframeArcProcessor`. **`reputation-queries.ts`**: resolved-market reputation transaction and user reputation read moved from engine and API `reputation-service` (optional trace labels preserve API query-monitor tags).
- **`prompt-context-queries.ts`**: recent world events (7-day window), rich-game-context event/post/question lists, and topic-diversity article rows moved from `context-builder`, `game-context-builder`, and `topic-diversity-service`. **`event-generation-queries.ts`**: world event insert and arc-pulse lookback moved from `event-generation-helpers`. **`createWorldEventRow`** accepts optional `timestamp` for simulation-aligned inserts. **`createWorldEventRowAsSystem`** wraps the same insert for callers that previously used **`DatabaseService.createEvent`** / **`getDbInstance().createEvent()`** (e.g. manual INT4 validation script).
- **`world-facts-generator-queries.ts`**: public events slice, active/resolved question slices, actor post slice, auto-generated fact insert, conditional archive, and stats aggregation moved from `world-facts-generator`.
- **`npc-interaction-tracker-queries.ts`**: NPC post author validation, agent close-trade stats, engagement social aggregation, top-engaged user id candidates, and per-user engaged NPC ids moved from `npc-interaction-tracker`. **`sub-market-queries.ts`**: try-spawn read, child/hierarchy/active lists, parent root lookup, arc/resolve updates, markets needing resolution, timeframed insert / parent child-count bump, org id lookup for question templates, and spawn log inserts moved from `sub-market-service`; **`insertSubMarketTimeframedMarketInTx`** and **`incrementSubMarketParentChildCountInTx`** centralize transactional SQL so `sub-market-service` no longer imports `timeframedMarkets` from runtime. Engine services that only needed **`withTransaction`** now import it from **`@babylon/db`** (same implementation as runtime).
- **`market-context-queries.ts`**: batched actor states, single-actor fetch (`fetchActorStateByIdForMarketContext`), all org states for perp snapshots (`listAllOrganizationStatesForMarketContext`), group chats/messages, relationships, pool positions, insider chat reads, recent feed/events/NPC posts, perp price history + open sizes, prediction market list, and signal market id lookup moved from `market-context-service` (no `getDbInstance()` there).
- **`narrative-arc-plan-queries.ts`**: question arc plan insert + fetch moved from `narrative-state-service` (replaces `questionArcPlan` repository calls). **`interaction-tracker-queries.ts`**: NPC–NPC interaction inserts moved from `InteractionTracker`. **`lookahead-queries.ts`**: latest post time, window post counts, continuous game start, active questions, top actor states, and per-author post aggregates moved from `lookahead-generation-service`.
- **GET /api/posts**: Enters the “following” feed branch only when the authenticated user matches the query `userId`; block/mute filters use `authUser?.userId` from auth, not from query params, so unauthenticated callers cannot leak or guess other users’ moderation state.
- **GET /api/registry** and **GET /api/registry/all**: Use `publicRateLimit()` instead of `optionalAuth()` alone; successful responses include rate limit and cache headers.
- **GET /api/onboarding/check-username**: Same pattern—`publicRateLimit()` supplies optional auth and rate limit info; headers attached on success.

### Developer notes

- When adding new public GET endpoints, call `publicRateLimit(request)` (or `publicRateLimit(request, 'firehose')` for SSE/token endpoints) at the start of the handler and use the returned `user` instead of calling `optionalAuth()` again. Always guard on `user` being null and call `addPublicReadHeaders(res, rateLimitInfo)` on successful responses when `rateLimitInfo` is present. See `packages/api/src/rate-limiting/README.md` for full usage and rationale.
